apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quarkus-ops.fullname" . }}-test-connection"
  labels:
    {{- include "quarkus-ops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quarkus-ops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
